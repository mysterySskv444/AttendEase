specVersion: "v1beta"
serviceId: "attendease"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "attendease-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
